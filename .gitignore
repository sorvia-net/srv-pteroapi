# ── Sorvia: derleme ciktilari ──
node_modules/
vendor/
*.log

# ── Sorvia: sirlar — bunlar depoya ASLA girmemeli ──
.env
.env.*
!.env.example
*.pem
*.key
.npmrc

# ── Sorvia: paketlenmis cikti ──
# .zip ara ciktidir. .blueprint ise dagitilan dosyanin kendisi ve depoda
# duruyor: "blueprint -install" klasor degil o dosyayi ariyor, klonlayan
# kisi ayrica paketlemek zorunda kalmasin.
*.zip

# ── Sorvia: yerel ──
.DS_Store
Thumbs.db
data/
